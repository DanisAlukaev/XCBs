# @package _global_

defaults:
  - override /model: M18
  - override /dataset: shapes

name: E22
epoch_freeze_backbone: 2
epoch_reinitialize: 2
max_epochs: 10
dataset:
  collate_fn:
    _target_: datasets.collators.CollateIndices
    vocabulary:
      _target_: datasets.utils.VocabularyShapes
      annotation_path: /home/danis/Projects/AlphaCaption/AutoConceptBottleneck/data/shapes/captions.csv
  num_workers: 10
  batch_size: 64
devices:
  - 0

model:
  direct_kl: true
